/** Nome do cookie do espaço "em manutenção" — compartilhado entre o seletor
 *  (cliente, grava) e a resolução por tela (servidor, lê). Sem `server-only`
 *  para poder ser importado no componente cliente. */
export const SPACE_COOKIE = "kb.admin.space";

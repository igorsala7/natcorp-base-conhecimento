# dependências
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# produção
/build

# testes
/coverage

# ambiente (segredos nunca versionados)
.env
.env.local
.env*.local

# arquivos de trabalho/temporários (scripts, prints, dumps — nunca versionar)
/temp/

# fluxos internos do n8n (estrutura da API de RH + chave de bootstrap — nunca versionar)
/n8n/

# supabase
/supabase/.branches
/supabase/.temp

# sistema / editor
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json
.idea

# typescript
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/.next-verify/
.claude/settings.local.json
